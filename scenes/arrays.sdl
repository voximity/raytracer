let verts = []

for y in 0 to 10 {
    for x in 0 to 10 {
        verts << <x, y, 0>
        verts << <x, y + 1, 0>
        verts << <x + 1, y, 0>
    }
}

mesh {
    verts,
    position: <0, 0, -10>
}
